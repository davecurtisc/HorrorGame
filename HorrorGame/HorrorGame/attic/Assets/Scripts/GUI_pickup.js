/*var pickupStyle : GUIStyle;

var ShowPickupGUI : boolean;

function Start () {
	ShowPickupGUI = false;
}

function Update () {
	if (ShowMouseGUI == true){
		GUI.Label (Rect (500,400,300,300),"Move the mouse to looks around", lookStyle);
		}
}
*/